#ifndef BUFFER_ENUMS_H
#define BUFFER_ENUMS_H

enum class BufferType {
	floatBuffer,   
	integerBuffe,
};

enum class DrawType {
	Static,
	Dynamic
};

enum class ArrayType {
	ArrayBuffer,
	ElementArrayBuffer
};

#endif